import {test, expect} from '@playwright/test';

test ('verify the user is able to login with valid crededentials', async () => {

await page.goto (' await page.goto(https://playwright.dev/');
await expect(page).toHaveTitle('/Playwright/');

})